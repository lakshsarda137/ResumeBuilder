import type { ResumeData } from '../types/resume';
import { PERSONAL } from '../personal';

/** The starter resume; its content lives in the personal profile. */
export const defaultResume: ResumeData = PERSONAL.defaultResume;
